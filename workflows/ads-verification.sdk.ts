// Leadgen — Ad Verification (warm-gated 'ads' fleet service). Active-Ad Detection Tier 2 / CONFIRMED.
// Deployed instance: <<WORKFLOW_ID>>.
// Claim 'ads' -> Get Target (business_name + website_domain + address) -> Meta Ad Library API (active
// creatives by name) + SerpApi google_ads_transparency_center (by domain) + SerpApi yelp (find_desc/
// find_loc) -> Build Evidence: ad_status {overall_is_advertising, tier:'CONFIRMED'|'checked',
// summary:{meta,google,yelp}, evidence_ledger:{live_ad_urls}} + ad_active (boolean) ->
// complete_analysis_work_item (cause ads_evidence) -> re-score. GATING: created 'blocked' at
// discovery; the Scorer opens it (blocked->pending) only when the lead is warm/hot
// (complete_scorer_work_item ads gate). Ships behind service_config.ads.
//
// MATCHING (ads-v2.1): a platform is CONFIRMED only when an ad's page/title contains EVERY significant
// token of the target's business name (len>=3, minus stopwords). Meta/Yelp/Google search results
// surface competitors bidding on the same terms, so first-word matching (e.g. a city name) produced
// false CONFIRMEDs — which would wrongly disqualify a good consultation lead. Each live_ad_url is
// pushed only when its own platform is confirmed. Google is domain-scoped (transparency center by
// website_domain) so it needs no name match.
//
// IDEMPOTENCY: ad_status/ad_active idempotency keys are version-pinned (':ads-v2.1'). The ledger's
// unique key is (campaign_id, service, idempotency_key) ON CONFLICT DO NOTHING, so each campaign gets
// one ads observation per business; a version bump appends a corrective observation (latest-wins).
//
// SECRETS: Meta Ad Library token inline in the Meta node (redacted <<META_TOKEN>> here); SerpApi via
// the "SerpApi account" credential (Google + Yelp arms).
// NEXTDOOR — LIKELY-only by design (no CONFIRMED arm): Nextdoor has no public ad transparency library
// (in-feed, neighbor-targeted ads can't be enumerated externally) and Apify's Nextdoor actors return
// business-directory data, not ads. Nextdoor advertising is covered as a LIKELY signal via the free
// `nextdoor_pixel` in the Website Auditor. A CONFIRMED arm was evaluated and dropped (only defensible
// source = active Local Deals, which needs an unproven paid actor with low yield + false-positive risk).
import { workflow, node, trigger, sticky, newCredential, expr } from '@n8n/workflow-sdk';

const everyMinute = trigger({ type: 'n8n-nodes-base.scheduleTrigger', version: 1.3, config: { name: 'Poll Ads Queue', position: [200, 240], parameters: { rule: { interval: [{ field: 'minutes', minutesInterval: 1 }] } } }, output: [{}] });
const poke = trigger({ type: 'n8n-nodes-base.webhook', version: 2.1, config: { name: 'Ads Poke', position: [200, 440], parameters: { httpMethod: 'POST', path: 'leadgen-ads-poke', responseMode: 'onReceived' } }, output: [{ body: {} }] });

const claim = node({ type: 'n8n-nodes-base.postgres', version: 2.6, config: { name: 'Claim Ads Work', position: [460, 320], executeOnce: true, parameters: { operation: 'executeQuery', query: "SELECT work_item_id, claim_token, service_run_id, campaign_id, campaign_lead_id FROM leadgen.claim_work_items('ads', $1)", options: { queryReplacement: expr('ads-{{ $execution.id }}') } }, credentials: { postgres: newCredential('Postgres account') } }, output: [{ work_item_id: 'wi', claim_token: 'ct', campaign_id: 'c', campaign_lead_id: 'l' }] });

const getTarget = node({ type: 'n8n-nodes-base.postgres', version: 2.6, config: { name: 'Get Target', position: [720, 320], parameters: { operation: 'executeQuery', query: "SELECT b.id AS business_id, b.business_name, b.website_domain, b.address FROM leadgen.campaign_leads cl JOIN leadgen.businesses b ON b.id=cl.business_id WHERE cl.id=$1", options: { queryReplacement: expr('{{ $json.campaign_lead_id }}') } }, credentials: { postgres: newCredential('Postgres account') } }, output: [{ business_id: 'b', business_name: 'X', website_domain: 'x.com', address: 'St, City' }] });

const metaAdLib = node({ type: 'n8n-nodes-base.httpRequest', version: 4.4, config: { name: 'Meta Ad Library', position: [980, 220], onError: 'continueRegularOutput', retryOnFail: true, maxTries: 2, waitBetweenTries: 2000, parameters: { method: 'GET', url: 'https://graph.facebook.com/v21.0/ads_archive', sendQuery: true, specifyQuery: 'keypair', queryParameters: { parameters: [ { name: 'search_terms', value: expr('{{ $json.business_name }}') }, { name: 'ad_reached_countries', value: '["US"]' }, { name: 'ad_active_status', value: 'ACTIVE' }, { name: 'fields', value: 'id,page_name,ad_snapshot_url' }, { name: 'limit', value: '10' }, { name: 'access_token', value: '<<META_TOKEN>>' } ] }, options: { timeout: 30000 } } }, output: [{ data: [] }] });

const googTransparency = node({ type: 'n8n-nodes-base.httpRequest', version: 4.4, config: { name: 'Google Transparency', position: [980, 420], onError: 'continueRegularOutput', retryOnFail: true, maxTries: 2, waitBetweenTries: 2500, parameters: { method: 'GET', url: 'https://serpapi.com/search.json', authentication: 'predefinedCredentialType', nodeCredentialType: 'serpApi', sendQuery: true, specifyQuery: 'keypair', queryParameters: { parameters: [ { name: 'engine', value: 'google_ads_transparency_center' }, { name: 'text', value: expr("{{ $('Get Target').item.json.website_domain || $('Get Target').item.json.business_name }}") } ] }, options: { timeout: 30000 } }, credentials: { serpApi: newCredential('SerpApi account') } }, output: [{ ad_creatives: [] }] });

const yelpAds = node({ type: 'n8n-nodes-base.httpRequest', version: 4.4, config: { name: 'Yelp Ads', position: [980, 600], onError: 'continueRegularOutput', retryOnFail: true, maxTries: 2, waitBetweenTries: 2500, parameters: { method: 'GET', url: 'https://serpapi.com/search.json', authentication: 'predefinedCredentialType', nodeCredentialType: 'serpApi', sendQuery: true, specifyQuery: 'keypair', queryParameters: { parameters: [ { name: 'engine', value: 'yelp' }, { name: 'find_desc', value: expr("{{ $('Get Target').item.json.business_name }}") }, { name: 'find_loc', value: expr("{{ $('Get Target').item.json.address || 'United States' }}") } ] }, options: { timeout: 30000 } }, credentials: { serpApi: newCredential('SerpApi account') } }, output: [{ ads_results: [] }] });

const bingAds = node({ type: 'n8n-nodes-base.httpRequest', version: 4.4, config: { name: 'Bing Ads', position: [980, 780], onError: 'continueRegularOutput', retryOnFail: true, maxTries: 2, waitBetweenTries: 2500, parameters: { method: 'GET', url: 'https://serpapi.com/search.json', authentication: 'predefinedCredentialType', nodeCredentialType: 'serpApi', sendQuery: true, specifyQuery: 'keypair', queryParameters: { parameters: [ { name: 'engine', value: 'bing' }, { name: 'q', value: expr("{{ $('Get Target').item.json.business_name }}") } ] }, options: { timeout: 30000 } }, credentials: { serpApi: newCredential('SerpApi account') } }, output: [{ ads: [] }] });

const buildEvidence = node({ type: 'n8n-nodes-base.code', version: 2, config: { name: 'Build Ads Evidence', position: [1240, 320], parameters: { mode: 'runOnceForEachItem', language: 'javaScript', jsCode: "const B=$('Get Target').item.json; const meta=$('Meta Ad Library').item.json||{}; const goog=$('Google Transparency').item.json||{}; const yelp=$('Yelp Ads').item.json||{}; const bing=$('Bing Ads').item.json||{}; const nm=((B.business_name||'')+'').toLowerCase().replace(/[^a-z0-9 ]/g,' ').replace(/\\s+/g,' ').trim(); const STOP={the:1,and:1,llc:1,inc:1,co:1,of:1,for:1}; const toks=nm.split(' ').filter(function(w){ return w.length>=3 && !STOP[w]; }); function matchName(t){ var s=((t||'')+'').toLowerCase(); if(!toks.length) return false; for(var i=0;i<toks.length;i++){ if(s.indexOf(toks[i])<0) return false; } return true; } const metaAds=Array.isArray(meta.data)?meta.data:[]; const metaMatch=metaAds.filter(function(a){ return a && matchName(a.page_name); }); const metaConfirmed=metaMatch.length>0; var metaUrls=[]; for(var i=0;i<metaMatch.length && metaUrls.length<3;i++){ if(metaMatch[i].ad_snapshot_url) metaUrls.push(metaMatch[i].ad_snapshot_url); } const googCreatives=Array.isArray(goog.ad_creatives)?goog.ad_creatives:(Array.isArray(goog.ads)?goog.ads:[]); const googConfirmed=googCreatives.length>0; var googUrl=(goog.search_metadata&&(goog.search_metadata.google_ads_transparency_center_url||goog.search_metadata.url))||null; var yelpAdsList=Array.isArray(yelp.ads_results)?yelp.ads_results:(Array.isArray(yelp.inline_ads)?yelp.inline_ads:[]); var yelpConfirmed=false; var yelpUrl=null; for(var y=0;y<yelpAdsList.length;y++){ var ya=yelpAdsList[y]||{}; if(matchName(ya.title||ya.name)){ yelpConfirmed=true; yelpUrl=ya.link||ya.link_href||null; break; } } var bingAdsList=Array.isArray(bing.ads)?bing.ads:(Array.isArray(bing.ads_results)?bing.ads_results:[]); var bingConfirmed=false; var bingUrl=null; for(var b=0;b<bingAdsList.length;b++){ var ba=bingAdsList[b]||{}; if(matchName(ba.title||ba.name||ba.snippet||ba.displayed_link)){ bingConfirmed=true; bingUrl=ba.link||ba.displayed_link||null; break; } } var liveUrls=metaUrls.slice(); if(googConfirmed && googUrl) liveUrls.push(googUrl); if(yelpConfirmed && yelpUrl) liveUrls.push(yelpUrl); if(bingConfirmed && bingUrl) liveUrls.push(bingUrl); const anyConf=metaConfirmed||googConfirmed||yelpConfirmed||bingConfirmed; const ad_status={ overall_is_advertising:anyConf, tier:(anyConf?'CONFIRMED':'checked'), summary:{ meta:(metaConfirmed?'CONFIRMED':'NONE'), google:(googConfirmed?'CONFIRMED':'NONE'), yelp:(yelpConfirmed?'CONFIRMED':'NONE'), bing:(bingConfirmed?'CONFIRMED':'NONE') }, evidence_ledger:{ live_ad_urls:liveUrls } }; const evidence=[ { feature_key:'ad_status', value:ad_status, value_type:'object', product_tag:'ads_video', source_provider:'ad_verification', idempotency_key:B.business_id+':ad_status:ads-v2.2' }, { feature_key:'ad_active', value:anyConf, value_type:'boolean', product_tag:'ads_video', source_provider:'ad_verification', idempotency_key:B.business_id+':ad_active:ads-v2.2' } ]; return { json:{ payload:{ evidence:evidence, cause_type:'ads_evidence', run:{ workflow_version:'ads-v2.2-bing' } } } };" } }, output: [{ payload: {} }] });

const complete = node({ type: 'n8n-nodes-base.postgres', version: 2.6, config: { name: 'Complete Ads', position: [1500, 320], parameters: { operation: 'executeQuery', query: "=SELECT leadgen.complete_analysis_work_item('{{ $('Claim Ads Work').item.json.work_item_id }}'::uuid, '{{ $('Claim Ads Work').item.json.claim_token }}'::uuid, '{{ JSON.stringify($json.payload).replace(/'/g, \"''\") }}'::jsonb) AS result" }, credentials: { postgres: newCredential('Postgres account') } }, output: [{ result: {} }] });

const note = sticky('## Ad Verification (warm-gated `ads` service — Meta + Google + Yelp + Bing)\n\nClaim ads -> Get Target -> Meta Ad Library + SerpApi Google Transparency + SerpApi Yelp + SerpApi Bing -> ad_status {tier CONFIRMED, summary{meta,google,yelp,bing}, live_ad_urls} + ad_active -> complete_analysis_work_item (cause ads_evidence) -> re-score.', [claim], { color: 5 });

export default workflow('leadgen-ads-verification', 'Leadgen — Ad Verification')
  .add(everyMinute).to(claim)
  .add(poke).to(claim)
  .add(claim).to(getTarget).to(metaAdLib).to(googTransparency).to(yelpAds).to(bingAds).to(buildEvidence).to(complete)
  .add(note);
