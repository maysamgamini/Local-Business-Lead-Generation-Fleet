// Leadgen — Review Miner v2 (US1 T037/T038 + Yelp). Deployed instance trDsKi1XVraj3b1i.
// Claim 'reviews' -> Get Target (business + address + campaign business_type) ->
// Fetch Reviews (Apify Google Maps reviews, newest 150) -> YELP ENRICH (one Code node:
// Yelp Fusion match by name+address -> business details rating/review_count/url; if the
// business has >=3 Yelp reviews, Apify tri_angle~yelp-review-scraper pulls the Yelp review
// TEXT — all via this.helpers.httpRequest) -> Compute Review Stats (MERGE Google+Yelp corpus;
// deterministic Google stats + yelp_rating/review_count/url + reputation_gap = googleRating -
// yelpRating; theme prompt over the combined corpus, temperature 0.2 for grounding) ->
// Extract Themes (Gemini) -> Build Reviews Evidence (Google stats + Yelp stats +
// reputation_gap + quote-checked combined complaint_themes; source-tagged) -> complete.
// Ephemeral corpus (FR-026): raw reviews (Google + Yelp) never persisted — only derived
// stats + verified short quotes. Degrades: Yelp no-match/scrape-fail -> Google-only; Gemini
// error -> Tier-1 still lands. Yelp Apify gated on review_count>=3 (cost control).
// SECRETS (redacted; real in the deployed node): <<GEMINI_KEY>> (Extract Themes URL);
//   <<YELP_KEY>> (Yelp Fusion Bearer) + <<APIFY_TOKEN>> (Google + Yelp scrapes) in code.
import { workflow, node, trigger, sticky, newCredential, expr } from '@n8n/workflow-sdk';

const everyMinute = trigger({ type: 'n8n-nodes-base.scheduleTrigger', version: 1.3, config: { name: 'Poll Reviews Queue', position: [200, 240], parameters: { rule: { interval: [{ field: 'minutes', minutesInterval: 1 }] } } }, output: [{}] });
const poke = trigger({ type: 'n8n-nodes-base.webhook', version: 2.1, config: { name: 'Reviews Poke', position: [200, 440], parameters: { httpMethod: 'POST', path: 'leadgen-reviews-poke', responseMode: 'onReceived' } }, output: [{ body: {} }] });

const claim = node({ type: 'n8n-nodes-base.postgres', version: 2.6, config: { name: 'Claim Reviews Work', position: [460, 320], executeOnce: true, parameters: { operation: 'executeQuery', query: "SELECT work_item_id, claim_token, service_run_id, campaign_id, campaign_lead_id FROM leadgen.claim_work_items('reviews', $1)", options: { queryReplacement: expr('reviews-{{ $execution.id }}') } }, credentials: { postgres: newCredential('Postgres account') } }, output: [{ work_item_id: 'wi', claim_token: 'ct', campaign_id: 'c', campaign_lead_id: 'l' }] });

const getTarget = node({ type: 'n8n-nodes-base.postgres', version: 2.6, config: { name: 'Get Target', position: [700, 320], parameters: { operation: 'executeQuery', query: "SELECT b.id AS business_id, b.place_id, b.business_name, b.website_domain, b.address, c.business_type FROM leadgen.campaign_leads cl JOIN leadgen.businesses b ON b.id=cl.business_id JOIN leadgen.campaigns c ON c.id=cl.campaign_id WHERE cl.id=$1", options: { queryReplacement: expr('{{ $json.campaign_lead_id }}') } }, credentials: { postgres: newCredential('Postgres account') } }, output: [{ business_id: 'b', place_id: 'ChIJ', business_name: 'Spa', address: '...', business_type: 'med spa' }] });

const fetchReviews = node({ type: 'n8n-nodes-base.httpRequest', version: 4.4, config: { name: 'Fetch Reviews', position: [900, 320], onError: 'continueRegularOutput', retryOnFail: true, maxTries: 2, waitBetweenTries: 5000, parameters: { method: 'POST', url: 'https://api.apify.com/v2/acts/compass~google-maps-reviews-scraper/run-sync-get-dataset-items?token=<<APIFY_TOKEN>>&timeout=200', sendBody: true, specifyBody: 'json', jsonBody: expr('={ "placeIds": ["{{ $json.place_id }}"], "maxReviews": 150, "reviewsSort": "newest", "language": "en" }'), options: { timeout: 230000, response: { response: { fullResponse: true } } } } }, output: [{ body: [{ text: 'great', stars: 5 }] }] });

// Yelp arm — one Code node (this.helpers.httpRequest): Fusion match -> details -> (gated) Apify scrape.
const yelpEnrich = node({ type: 'n8n-nodes-base.code', version: 2, config: { name: 'Yelp Enrich', position: [1080, 460], parameters: { mode: 'runOnceForEachItem', language: 'javaScript', jsCode: "const gbody=Array.isArray($json.body)?$json.body:(Array.isArray($json)?$json:[]); const tgt=$('Get Target').item.json; const YK='<<YELP_KEY>>'; const AP='<<APIFY_TOKEN>>'; const name=(tgt.business_name||''); const addr=(tgt.address||''); let address1='',city='',state=''; try{ const p=addr.split(',').map(s=>s.trim()); if(p.length>=3){ address1=p[0]; city=p[1]; state=((p[2]||'').split(/\\s+/)[0]||''); } }catch(e){} let yelp={matched:false,rating:null,review_count:null,url:null,reviews:[]}; try{ const m=await this.helpers.httpRequest({ method:'GET', url:'https://api.yelp.com/v3/businesses/matches', qs:{ name:name, address1:address1, city:city, state:state, country:'US', limit:1 }, headers:{ Authorization:'Bearer '+YK }, json:true }); const biz=(m&&m.businesses&&m.businesses[0])||null; if(biz&&biz.id){ const det=await this.helpers.httpRequest({ method:'GET', url:'https://api.yelp.com/v3/businesses/'+biz.id, headers:{ Authorization:'Bearer '+YK }, json:true }); yelp.matched=true; yelp.rating=(det.rating!=null?det.rating:null); yelp.review_count=(det.review_count!=null?det.review_count:null); yelp.url=det.url||('https://www.yelp.com/biz/'+biz.alias); if((yelp.review_count||0)>=3 && yelp.url){ try{ const ap=await this.helpers.httpRequest({ method:'POST', url:'https://api.apify.com/v2/acts/tri_angle~yelp-review-scraper/run-sync-get-dataset-items?token='+AP+'&timeout=170', body:{ startUrls:[{url:yelp.url}], maxReviewsPerBusiness:100 }, json:true, timeout:190000 }); if(Array.isArray(ap)) yelp.reviews=ap.map(r=>({text:(r.text||'').toString(), rating:r.rating, owner:!!(r.ownerResponse)})).filter(r=>r.text); }catch(e){ yelp.apify_error=(''+e).slice(0,120); } } } }catch(e){ yelp.error=(''+e).slice(0,150); } return { json:{ body:gbody, yelp:yelp } };" } }, output: [{ body: [], yelp: {} }] });

const computeStats = node({ type: 'n8n-nodes-base.code', version: 2, config: { name: 'Compute Review Stats', position: [1300, 320], parameters: { mode: 'runOnceForEachItem', language: 'javaScript', jsCode: "const resp=$json; const yelp=(resp&&resp.yelp)||{}; const g=Array.isArray(resp.body)?resp.body:[]; const n=g.length; const num=x=>typeof x==='number'&&isFinite(x); let gRating=null,gCount=null; if(n>0){ gRating=num(g[0].totalScore)?g[0].totalScore:null; gCount=num(g[0].reviewsCount)?g[0].reviewsCount:null; } const stars=g.map(r=>r.stars).filter(num); const avg=a=>a.length?a.reduce((x,y)=>x+y,0)/a.length:null; const avgStars=avg(stars); const withResp=g.filter(r=>r.responseFromOwnerText&&(''+r.responseFromOwnerText).trim()).length; const ownerRate=n?+(withResp/n).toFixed(3):null; const third=Math.max(1,Math.floor(stars.length/3)); const traj=(stars.length>=3)?+((avg(stars.slice(0,third))||0)-(avg(stars.slice(-third))||0)).toFixed(2):null; const gTexts=g.map(r=>(r.text||'').replace(/\\s+/g,' ').trim()).filter(Boolean); const yTexts=(Array.isArray(yelp.reviews)?yelp.reviews:[]).map(r=>((r.text||'')+'').replace(/\\s+/g,' ').trim()).filter(Boolean); const corpus=(gTexts.concat(yTexts)).join(' || ').toLowerCase(); const gRatingFinal=gRating!=null?gRating:avgStars; const repGap=(gRatingFinal!=null&&yelp.rating!=null)?+(gRatingFinal-yelp.rating).toFixed(2):null; const gSample=gTexts.slice(0,45).map((t,i)=>'[G'+i+'] '+t.slice(0,350)).join('\\n'); const ySample=yTexts.slice(0,25).map((t,i)=>'[Y'+i+'] '+t.slice(0,350)).join('\\n'); const bn=($('Get Target').item.json.business_name||''); const prompt='You are analyzing REAL customer reviews of a local business ('+bn+') from Google [G] and Yelp [Y] to find sales angles for our marketing services: web_seo (website redesign), voice_ai (AI phone receptionist), chatbot (website support chatbot for repeated FAQs), social (social media automation: reply to comments/DMs + content), ads_video (visibility/reputation), consulting (operations). Use ONLY what appears in the reviews; never invent. Return ONLY compact JSON: {themes:[{product_tag, theme (short), severity (low|med|high), quotes:[verbatim snippets copied exactly]}], phone_scheduling_complaint_reviews:<int>, total_complaint_reviews:<int>}. Reviews:\\n'+gSample+'\\n'+ySample; const geminiBody={ contents:[{parts:[{text:prompt}]}], generationConfig:{ responseMimeType:'application/json', maxOutputTokens:1400, temperature:0.2, thinkingConfig:{thinkingBudget:0} } }; return { json:{ n_reviews:n+yTexts.length, review_volume:gCount!=null?gCount:n, rating:gRatingFinal, owner_response_rate:ownerRate, trajectory:traj, yelp_rating:(yelp.rating!=null?yelp.rating:null), yelp_review_count:(yelp.review_count!=null?yelp.review_count:null), yelp_url:(yelp.url||null), reputation_gap:repGap, corpus:corpus, geminiBody:geminiBody } };" } }, output: [{ geminiBody: {} }] });

const extractThemes = node({ type: 'n8n-nodes-base.httpRequest', version: 4.4, config: { name: 'Extract Themes', position: [1520, 320], onError: 'continueRegularOutput', retryOnFail: true, maxTries: 2, waitBetweenTries: 5000, parameters: { method: 'POST', url: 'https://generativelanguage.googleapis.com/v1beta/models/gemini-flash-latest:generateContent?key=<<GEMINI_KEY>>', sendBody: true, specifyBody: 'json', jsonBody: expr('={{ $json.geminiBody }}'), options: { timeout: 60000, batching: { batch: { batchSize: 1, batchInterval: 2500 } } } } }, output: [{ candidates: [] }] });

const buildEvidence = node({ type: 'n8n-nodes-base.code', version: 2, config: { name: 'Build Reviews Evidence', position: [1740, 320], parameters: { mode: 'runOnceForEachItem', language: 'javaScript', jsCode: "const stats=$('Compute Review Stats').item.json; const vis=$json; const claim=$('Claim Reviews Work').item.json; const tgt=$('Get Target').item.json; const cid=claim.campaign_id, bid=tgt.business_id; function mk(fk,val,vt,unit,tag){ return {feature_key:fk,value:val,value_type:vt,unit:unit,product_tag:tag,source_provider:'review_miner',idempotency_key:cid+':'+bid+':'+fk}; } const ev=[]; /* review_volume intentionally NOT emitted here: Discovery's Places user_ratings_total is the authoritative review count; the Apify google-reviews scrape returns 0/null on empty or capped runs and would clobber it (bug fixed 2026-07-18). */ if(stats.rating!=null) ev.push(mk('rating',stats.rating,'decimal',null,'ads_video')); if(stats.owner_response_rate!=null){ ev.push(mk('owner_response_rate',stats.owner_response_rate,'decimal',null,'voice_ai')); ev.push(mk('owner_responds_to_reviews', stats.owner_response_rate>=0.2,'boolean',null,'consulting')); } if(stats.trajectory!=null) ev.push(mk('review_trajectory',stats.trajectory,'decimal',null,'ads_video')); if(stats.yelp_rating!=null) ev.push(mk('yelp_rating',stats.yelp_rating,'decimal',null,'ads_video')); if(stats.yelp_review_count!=null) ev.push(mk('yelp_review_count',stats.yelp_review_count,'integer','count','ads_video')); if(stats.reputation_gap!=null) ev.push(mk('reputation_gap',stats.reputation_gap,'decimal',null,'ads_video')); if(stats.yelp_url) ev.push({feature_key:'yelp_url',value:stats.yelp_url,value_type:'string',product_tag:'ads_video',source_provider:'review_miner',idempotency_key:cid+':'+bid+':yelp_url'}); let v={}; try{ let raw=''; const cand=vis&&vis.candidates&&vis.candidates[0]; if(cand&&cand.content&&cand.content.parts&&cand.content.parts[0]) raw=(cand.content.parts[0].text||'').toString().trim(); raw=raw.split('```json').join('').split('```').join('').trim(); const m=raw.match(/\\{[\\s\\S]*\\}/); if(m) raw=m[0]; v=JSON.parse(raw); }catch(e){ v={}; } const corpus=stats.corpus||''; function verify(q){ if(!q) return false; const s=(''+q).replace(/\\s+/g,' ').trim().toLowerCase(); if(s.length<12) return false; if(corpus.indexOf(s)!==-1) return true; const w=Math.min(s.length,45); for(let i=0;i+w<=s.length;i+=8){ if(corpus.indexOf(s.substr(i,w))!==-1) return true; } return false; } let verifiedThemes=[]; if(Array.isArray(v.themes)){ for(const th of v.themes){ const vq=(th.quotes||[]).filter(verify).slice(0,3); verifiedThemes.push({product_tag:th.product_tag, theme:th.theme, severity:th.severity, quotes:vq, quote_verified:vq.length>0}); } } if(stats.n_reviews>0 && (v.phone_scheduling_complaint_reviews!=null)){ const share=Math.max(0,Math.min(1,(Number(v.phone_scheduling_complaint_reviews)||0)/stats.n_reviews)); ev.push(mk('phone_complaint_share',+share.toFixed(3),'decimal',null,'voice_ai')); } if(verifiedThemes.length){ ev.push({feature_key:'complaint_themes', value:{ themes:verifiedThemes, total_complaint_reviews:(v.total_complaint_reviews!=null?v.total_complaint_reviews:null), n_reviews:stats.n_reviews, sources:{google:true, yelp:(stats.yelp_review_count!=null)} }, value_type:'object', product_tag:'voice_ai', source_provider:'review_miner', idempotency_key:cid+':'+bid+':complaint_themes', excerpt:(verifiedThemes.filter(t=>t.quotes.length).map(t=>t.theme).slice(0,3).join('; ')).slice(0,500)}); } return { json:{ payload:{ cause_type:'reviews_evidence', evidence:ev, run:{ workflow_version:'review-miner-v2-yelp' } } } };" } }, output: [{ payload: {} }] });

const complete = node({ type: 'n8n-nodes-base.postgres', version: 2.6, config: { name: 'Complete Reviews', position: [1960, 320], parameters: { operation: 'executeQuery', query: "SELECT leadgen.complete_analysis_work_item('{{ $('Claim Reviews Work').item.json.work_item_id }}'::uuid, '{{ $('Claim Reviews Work').item.json.claim_token }}'::uuid, '{{ JSON.stringify($json.payload).replace(/'/g, \"''\") }}'::jsonb) AS result", options: {} }, credentials: { postgres: newCredential('Postgres account') } }, output: [{ result: {} }] });

const note = sticky('## Review Miner v2 (Google + Yelp)\n\nApify Google reviews + Yelp (Fusion match/details + Apify tri_angle scrape, gated on >=3 Yelp reviews) -> merged corpus -> Gemini theme extraction (temp 0.2) with quote-checker -> typed evidence incl. yelp_rating, yelp_review_count, yelp_url, reputation_gap (Google-vs-Yelp delta) + combined complaint_themes. Ephemeral corpus (FR-026).', [claim], { color: 5 });

export default workflow('leadgen-review-miner', 'Leadgen — Review Miner')
  .add(everyMinute).to(claim)
  .add(poke).to(claim)
  .add(claim).to(getTarget).to(fetchReviews).to(yelpEnrich).to(computeStats).to(extractThemes).to(buildEvidence).to(complete)
  .add(note);
